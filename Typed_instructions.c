#include<stdio.h>
int main(){
    // area of square
    // float side;
    // printf("Enter The Side Of Square");
    // scanf("%f",&side);
    // float area= side*side;
    // printf("The area of square is %f", area);

    // area of circle
    float circleRadius;
    printf("Enter Radius");
    scanf("%f",&circleRadius);
    float area = 3.14*(circleRadius*circleRadius);
    printf("The area is %f", area);

    
}